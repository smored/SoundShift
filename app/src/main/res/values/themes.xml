<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SoundShift" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SoundShift" parent="Base.Theme.SoundShift">
        <item name="colorPrimary">@color/forePurple</item>
        <item name="colorPrimaryDark">@color/backPurple</item>
        <item name="colorAccent">@color/foreGrey</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAppearanceButton">@color/white</item>
        <item name="android:windowBackground">@color/grey</item>
        <item name="android:progressTint">@color/forePurple</item>
    </style>
</resources>